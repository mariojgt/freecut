{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Core permissions for the FreeCut editor window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
